## 2026-08-27 16:57

## 代码审查报告

### 审查目标
- 类型：本地改动 + 既有课程管理页产品契合度审查
- 范围：`apps/web/src/pages/attendance/courses.vue`（对照 `course_spus` / `course_skus` / `course_enrollments` 模型、迁移 034、盈達教育中心 June 时间表海报）
- 审查时间：2026-08-27 16:57

### 审查输入证据
- Diff 范围：`courses.vue` 相对 HEAD 的未提交改动（学生下拉改为页面加载预取 20 名有效学生 + 搜索竞态保护 + `no-filter`）
- 自动化检查：未跑 lint / typecheck / test。本次结论以产品契合度与数据模型为主，不作为 PR 回归证明。
- 其他上下文：用户明确要用该页支撑学费发票；海报为盈達教育中心 6 月课表（功課輔導班 + A1–E2 平日进修班 + F1–F8 周六班）。系统内无 attendance 发票 API；`/apps/invoice` 为模板假数据。最接近的「按月生成」模式是 staff payroll。

### 问题清单

#### 🔴 严重问题
**问题1：本页无法生成发票**
- 位置：`apps/web/src/pages/attendance/courses.vue`（整页）；attendance API 无 invoice 路由
- 问题：页面只做科目/开班 CRUD 与「先选学生再报名」。没有账单期、发票实体、行项目、已出账状态。Vuexy `apps/invoice` 未接到课程报名。
- 风险：若把当前报名表当发票来源，金额、月份、历史价格都无法保证正确。
- 建议：保留本页作目录；新增独立「Generate invoice」流程（对齐 payroll：选月份 → 按重叠报名拉学生 → 按 SKU 组行项目）。

**问题2：价格只存在 SKU 现价，没有快照**
- 位置：`apps/api/app/models/course_sku.py` `price`；报名表无金额字段
- 问题：发票若直接读 `course_skus.price`，之后改 A1 学费会改写历史账单。
- 风险：对账与补印旧月发票错误。
- 建议：出账时把单价、科目名、班次代码写入发票行；报名层可选 `agreed_price`。

**问题3：`(unit_id, sku_id)` 永久唯一**
- 位置：`apps/api/app/models/course_enrollment.py` `uq_course_enrollment_unit_sku`；`course_enrollments.py` 409
- 问题：同一学生不能再次报名同一班次（下学年 A1）。取消后重报仍冲突，除非删除旧行。
- 风险：新学期无法报名，或被迫删历史导致无法追溯旧发票。
- 建议：唯一约束改为「同一学生同一 SKU 仅一条 active」，或加入学期/学年维度。

#### 🟡 建议修改
**问题4：UI 未使用报名起迄日**
- 位置：`courses.vue` `enrollStudent()`；API `start_date` / `end_date` 已存在
- 问题：报名只提交 `unit_id` + `sku_id`。无法按「2026-06」过滤谁该缴费。
- 建议：报名表单补开始/结束日期；发票查询按日期窗口与 `status=active` 过滤。

**问题5：报名 UX 是学生优先，且默认 20 人**
- 位置：`courses.vue:316-326`、`681-772`
- 问题：海报运营是「这个时段有哪些学生」（班次名册）。当前是搜索单个学生。预取 20 名会让人误以为名单完整。
- 建议：主视图改为按 SKU 名册；学生搜索保留为次路径。不要为了发票去一次拉全校学生进下拉框。

**问题6：功課輔導班与进修班课时密度不同，却共用一个 `price`**
- 位置：`course_skus.price` + `schedule_note` 自由文本
- 问题：HW1 周一至五每天上课；A1 仅周一。无法从模型推出「6 月多少堂、如何计价」。
- 建议：明确计价单位（月费 / 堂费），或增加 `billing_unit`；不要用 `schedule_note` 做计算。

#### 🟢 可选优化
**问题7：学生/报名加载失败只打 console**
- 位置：`courses.vue` `loadStudentOptions`、`watch(selectedStudentId)`
- 建议：把错误显示到页面，与 `loadError` / `enrollError` 一致。

**问题8：海报目录不必重做 UI**
- 位置：SPU/SKU 表格
- 建议：用 4 个 SPU + 约 20 个 SKU（代码=海报班号）即可。课表可视化可后做。

### 亮点
- SPU/SKU 分层与海报一致：科目（英文/中文/数学/功课辅导）对 SPU，班号（A1、F5）对 SKU。
- 报名 API 已能按 `unit_id` 或 `sku_id` 过滤，班次名册不需要新列表接口。
- 未提交改动里的搜索 requestId 与 `no-filter` 方向正确（服务端搜索），只是「预载 20 人」容易误导。
- Staff payroll 已有「选月份再生成」可复用到学费发票，不必从 Vuexy 假发票抄。

### 结论
- 结论：`Request Changes`
- 严重问题：3 个
- 建议修改：3 个
- 可选优化：2 个
- 下一步建议：先确认计价规则（月费还是堂费、功课辅导是否与 A1 不同价）；再拆两件事——（1）课程页改成班次名册并补起迄日，（2）新做按月生成发票，不要把发票按钮塞进当前 `courses.vue`。

---

## 2026-08-27 跟进（已落地，不改写上文审查原文）

审查当时的缺口多数已实作。原文保留为当时证据。

| 审查项 | 现状 |
| ------ | ------ |
| 问题1 无法生成发票 | ✅ `tuition_invoices` + `POST /api/tuition-invoices/generate` + Web `/attendance/invoices`。Vuexy `/apps/invoice` 仍是模板假数据。 |
| 问题2 价格无快照 | ✅ 行项目写入 `sku_code` / `name_zh` / `billing_unit` / `unit_price` / `quantity` / `amount`。 |
| 问题3 `(unit_id, sku_id)` 永久唯一 | ⬜ 刻意未改。见 known-gaps **#M22**。 |
| 问题4 未用起迄日 | ✅ 名册报名提交 `start_date` / `end_date`；Generate 按月份重叠过滤。 |
| 问题5 学生优先 UX | ✅ 班次优先名册；学生搜索仍分页 20 人。 |
| 问题6 单一 price | ✅ SKU `billing_unit`：`monthly` / `per_session`。堂费按上课日∩出勤计堂（**#M23** 剩余假期日历）。 |
| WhatsApp 发送账单 | ⬜ 刻意未做（**#M24**）。 |

Migration head：**037**。测试：`test_courses.py`、`test_tuition_invoices.py`。尚未做的三项集中写在 [known-gaps.md](known-gaps.md) 文首表（#M22–#M24）。
---

## 2026-08-28 14:25

## 代码审查报告

### 审查目标
- 类型：本地改动
- 范围：SKU `meeting_weekdays` + 堂费 Generate 按上课日计堂（相对 `98733a5` 的未提交改动，含未追踪 `037_add_sku_meeting_weekdays.py`）
- 审查时间：2026-08-28 14:25

### 审查输入证据
- Diff 范围：`git diff HEAD -- apps/api apps/web/src docs` + untracked Alembic 037
- 自动化检查：通过 — `python -m pytest tests/test_tuition_invoices.py tests/test_courses.py -q` → 38 passed；`python -m alembic upgrade head` → 036 → 037
- 其他上下文：约定规则为上课日计堂、空列表 qty 1、qty 0 跳过、不减假期、不减缺席。审查员 [Review](90badb09-86e0-423c-9788-4dc6680cb3f8)

### 问题清单

#### 🔴 严重问题
无。

#### 🟡 建议修改
**问题1：Generate 跳过未启用 SKU（需产品确认，不回退）**
- 位置：`apps/api/app/services/tuition_invoice_generator.py`（`CourseSku.is_active.is_(True)`）
- 问题：本轮上课日需求未写 `is_active`，但上一轮审查已约定 inactive SKU 不出账。停用班次会让仍在籍学生的 **draft** 被删掉。
- 建议：保留过滤；在 SKU Active 勾选旁写明「停用后 Generate 不再出账，已 issued/paid 保留」。

**问题2：空上课日在裁剪窗口之前就回传 1**
- 位置：`apps/api/app/services/tuition_invoice_generator.py` `count_scheduled_sessions`
- 问题：倒挂起迄日（建立/PATCH 已禁）若仍入库，空上课日会出 1 堂、有上课日会出 0。
- 建议：先 clip，`start > end` 回 0，再处理空上课日。

#### 🟢 可选优化
**问题3：发票确认对话框 `v-else` 可能闪 void 文案**
- 位置：`apps/web/src/pages/attendance/invoices.vue`
- 建议：改为 `v-else-if="pendingStatus?.status === 'void'"`。

**问题4：ORM 缺 `meeting_weekdays` server_default；未知星期字串当未设定**
- 位置：`course_sku.py` model vs 037
- 建议：model 补 `server_default`；Generate 维持 API 校验路径即可。

### 亮点
- 日期窗口含端点、June 2026 黄金值正确；空列表 vs 0 堂分流清楚；`schedule_note` 不算堂。
- 测试覆盖计堂、裁剪、月费忽略、qty 0 跳过。
- 报名 PATCH 合并起迄日后再校验。

### 结论
- 结论：`Request Changes`（🟡 裁剪顺序须修；inactive SKU 保留并写 UI 说明）
- 严重问题：0 个
- 建议修改：2 个
- 可选优化：2 个
- 下一步建议：修裁剪顺序与对话框后做缺席扣堂。
---

## 2026-08-28 15:35

## 代码审查报告

### 审查目标
- 类型：本地改动（复审：后端 + 前端 + 计费业务逻辑全量）
- 范围：学费发票端到端——SKU 计价、`meeting_weekdays`、Generate、报名名册、`/attendance/invoices`；对照 HEAD `98733a5` 的未提交工作树（含未追踪 Alembic **037**）
- 审查时间：2026-08-28 15:35

### 审查输入证据
- Diff 范围：`git diff HEAD` 于 `apps/api`、`apps/web/src/pages/attendance`、`apps/web/src/api/attendance`、`docs`；未追踪 `apps/api/alembic/versions/037_add_sku_meeting_weekdays.py`
- 自动化检查：通过 — `python -m pytest tests/test_tuition_invoices.py tests/test_courses.py -q` → **46 passed**（约 50s）。未跑全量 pytest / web typecheck。浏览器端到端未点过（本环境无浏览器工具）。
- 其他上下文：独立审查员 [Review](5256f0ce-5169-4f79-a947-25084df0ebb7)（复审当时 45 passed；随后补 PATCH 清空上课日测试、课程列表分页、手册脚注）。上一轮 Important（堂费空上课日可保存、未启用 SKU 仍可报名）已在本轮落地。刻意接受：#M22 / #M23 假期表 / #M24。

### 历史问题关闭（相对 14:25 与上一轮产品审查）

| 项 | 状态 |
| --- | --- |
| 堂费空 `meeting_weekdays` 仍可 POST/PATCH | ✅ 新建/改成堂费须 ≥1 天；清空堂费上课日 422 |
| UI `skuCanSave` 不拦上课日 | ✅ 堂费无上课日不可 Save |
| 未启用 SKU 仍可报名 | ✅ POST 422；名册 Enroll 禁用 + Inactive chip |
| 裁剪窗口先于空上课日 fallback | ✅ 倒挂窗口 → 0（既有单测） |
| 发票确认 `v-else` 闪 void 文案 | ✅ 已是 `v-else-if` void |
| ORM `meeting_weekdays` server_default | ✅ model 已有 |
| 手册 ER「缺席未扣」 | ✅ 已改为未到已扣、假期未减 |
| 课程页只载入 100 个 SPU/SKU | ✅ `listCourseSpus` / `listCourseSkus` 按 200 分页拉完 |

### 业务逻辑（对照实现，无计费公式错误）

- **计价在 SKU**：`monthly` 月费 qty 1（忽略上课日与出勤）；`per_session` 堂费 qty = 裁剪后上课日 ∩ 该据点非作废出勤（香港日历日 `attendance_date`）。
- **窗口**：`[max(月初, enroll start), min(月末, enroll end)]`；起 > 迄 → 0 堂，跳过该行。
- **空上课日**：仅旧资料 fallback qty 1 且不看出勤；API 新建堂费已禁止。qty 0 不出 $0 行。
- **Generate**：只收 `active` 报名；跳过 cancelled/completed、区间外、无价、未启用 SKU。issued/paid 不覆盖。draft 替换行。无有效行的 draft 删除。void 无报名则保留，有报名则复活为 draft。唯一 `(unit_id, period_start, period_end)` → IntegrityError 409。
- **不从 `schedule_note` 算堂。** Vuexy `/apps/invoice` 仍是模板假数据。

### 问题清单

#### 🔴 严重问题
**问题1：Alembic 037 仍未纳入版本库**
- 位置：`apps/api/alembic/versions/037_add_sku_meeting_weekdays.py`（git `??`）
- 问题：ORM 已要求 `course_skus.meeting_weekdays`。文档已写 head **037**。功能几乎全在工作树，HEAD `98733a5` 仍把堂费当成 qty 1。
- 风险：合并/部署若漏这个文件，生产停在 036，SKU 查询失败。
- 建议：与 model / generator / 前端 **同一 commit** 加入 037。未要求提交前不合并。

#### 🟡 建议修改
**问题2：名册报名一次最多 200 人**
- 位置：`apps/web/src/pages/attendance/courses.vue` `listCourseEnrollmentsWithTotal({ page_size: 200 })`
- 问题：SPU/SKU 已拉全页；名册仍单页 200。超过则 Generate 仍会出账，但名册上看不见。
- 建议：与发票 `listAllTuitionInvoices` 同样循环拉完。

**问题3：Generate 覆盖 draft 无二次确认**
- 位置：`apps/web/src/pages/attendance/invoices.vue` `generate()`
- 问题：Issue/Paid/Void 有确认；Generate 直接重写该月 draft 并可能删过期 draft。
- 建议：有 draft 时弹确认（产品可延后）。

**问题4：报名不校验 `capacity`**
- 位置：`apps/api/app/routers/course_enrollments.py` POST
- 问题：UI 显示 `n / capacity`，API 不挡超额。
- 建议：超额 422，或明确容量仅展示。

**问题5：未启用 SKU 上仍可 PATCH 报名回 `active`**
- 位置：`course_enrollments.py` PATCH；Generate 仍跳过该 SKU，不会多收费
- 建议：PATCH 恢复 active 时同样检查 SKU `is_active`。

#### 🟢 可选优化
**问题6：同据点同日多堂费班共用一次打卡** — 设计如此（校园日相交）；两晚班同楼需口头说明。
**问题7：SKU `location_id` 为空则任何校园打卡都算堂** — UI 有提示；运营应强制选据点。
**问题8：月费不按日折；起迄日皆空则每月都出** — `enrolled_at` 不参与 Generate。
**问题9：SPU `is_active` 不影响 Generate** — 只看 SKU。
**问题10：409 单测为 monkeypatch IntegrityError** — 非双请求真撞唯一约束。
**问题11：缺少时区边界出勤单测**（例如 `2026-06-14T16:30:00Z` → 香港周一）。
**问题12：无报名时 void 保持 void 无对应测试**（复活路径已测）。
**问题13：手册部分段落仍写旧测试数量（5 / 38）**；以 46 为准。
**问题14：已 paid 不能 void** — 状态机刻意锁定。

### 亮点
- 计费公式、裁剪、香港日出勤相交、draft/lock/void/409 与需求一致。
- 本轮校验闭环：创建 / PATCH 合并态 / 报名 / 前端 Save 与 Enroll。
- 行项目快照价格与班名；发票「To collect」不含 void；分页拉完发票。
- 堂费 qty 0 跳过，避免 $0 账单。

### 结论
- 结论：`Request Changes`（仅因 **037 必须与功能同 commit**；无计费公式 🔴）
- 严重问题：1 个（发布流程 / migration 未入库）
- 建议修改：4 个
- 可选优化：9 个
- 下一步建议：提交时务必 `git add` 037 与相关代码。名册 200 人上限与 Generate 确认可下一轮。#M22–#M24 不要在本功能重开。
---

